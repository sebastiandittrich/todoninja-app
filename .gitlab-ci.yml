stages:
  - build
  - test

image: node:8.12

build:
  stage: build
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - dist


test:
  stage: test
  script:
    - pwd
    - ls -la